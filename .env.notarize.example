# macOS Notarization設定（本番ビルド時に使用）
# 使用方法: このファイルを .env.notarize にコピーして実際の値を入力してください

# Apple ID（Apple Developer Accountのメールアドレス）
APPLE_ID=your-apple-id@example.com

# App-Specific Password（Apple IDで生成したアプリ固有パスワード）
# 生成方法: https://appleid.apple.com → App用パスワード
# または Keychainに保存した場合: @keychain:AC_PASSWORD
APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Team ID（Apple Developer Accountで確認）
APPLE_TEAM_ID=YOUR_TEAM_ID

# ビルドコマンド例:
# source .env.notarize
# npm run build && npx electron-builder --mac
